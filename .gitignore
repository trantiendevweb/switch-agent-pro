# bản sao lưu do tk dong-bo sinh ra
*.bak-*
*.tmp

# rác Windows
Thumbs.db
desktop.ini

# python
__pycache__/
*.pyc

# go build artifacts
*.exe
/sagent
/dist/

# log của remote-control server chạy tại chỗ
.rc-server.log

# Trạng thái do chính công cụ sinh ra khi chạy thử trong repo. `.grok/` do Grok CLI
# tạo ở THƯ MỤC LÀM VIỆC — kiểm rồi, bản này không chứa key, nhưng cấu hình của
# provider là chỗ key hay nằm nên chặn từ đầu chứ đừng đợi lần sau.
.grok/

# Worktree tạm của workflow — không thuộc repo.
.claude/worktrees/
